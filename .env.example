# MinIO (S3-compatible) — defaults match docker-compose.yml
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=load-optimization
MINIO_SECURE=false

# Set to true to skip MinIO (no history / auto-save)
# MINIO_DISABLE=true
